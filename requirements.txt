# Phase 0 — core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# Phase 1 — ingestion
langchain>=0.3.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0
qdrant-client>=1.12.0
sentence-transformers>=3.3.0
pypdf>=5.0.0
pdfplumber>=0.11.0
python-docx>=1.1.0

# Phase 2 — RAG engine
langgraph>=0.2.0
langchain-groq>=0.2.0
httpx>=0.27.0
streamlit>=1.40.0

# Phase 3 — reranker
flashrank>=0.2.0

# Phase 4 — guardrails (RAIL_INDICATORS fast-path + llama-3.1-8b-instant LLM gate)
# nemoguardrails>=0.10.0  # optional: uncomment for full NeMo Colang runtime

# Phase 5 — gateway & observability
portkey-ai>=1.9.0
langchain-openai>=0.3.0
logfire[fastapi]>=2.0.0

# Phase 6 — evaluation
ragas>=0.2.0
# datasets>=3.0.0
